<header class="site-header">
<div class="site-header__inner">
<a href="/" class="site-title">{{metadata.navbar.title}}</a>
<nav class="site-nav">
<ul class="site-nav__list">
{% for n in metadata.navbar.list %}
{% set normalizedNavUrl = n.url | replace("/", "") %}
{% set normalizedPageUrl = page.url | replace("/", "") %}
<li class="site-nav__item">
<a href="{{ n.url }}" class="site-nav__link {% if normalizedPageUrl == normalizedNavUrl %}active{% endif %}">
{{ n.nav }}
</a>
</li>
{% endfor %}
<li class="site-nav__item theme-toggle-container">
<button id="theme-toggle" class="theme-toggle-btn" aria-label="Toggle dark/light mode">
<span class="theme-toggle-dark">🌙</span>
<span class="theme-toggle-light">☀️</span>
</button>
</li>
</ul>
</nav>
</div>
</header>